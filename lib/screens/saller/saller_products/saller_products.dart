import 'package:flutter/material.dart';

class SallerProducts extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('saller Product'),
      ),
    );
  }
}